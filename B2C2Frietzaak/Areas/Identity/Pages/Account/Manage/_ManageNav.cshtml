@inject SignInManager<IdentityUser> SignInManager
@{
    var hasExternalLogins = (await SignInManager.GetExternalAuthenticationSchemesAsync()).Any();
}
<ul class="nav nav-pills flex-column">
    <li class="nav-item"><a class="btn btn-outline-light @ManageNavPages.IndexNavClass(ViewContext)" style="width: 300px" id="profile" asp-page="./Index">Profile</a></li>
    <li class="nav-item"><a class="btn btn-outline-light @ManageNavPages.EmailNavClass(ViewContext)" style="width: 300px" id="email" asp-page="./Email">Email</a></li>
    <li class="nav-item"><a class="btn btn-outline-light @ManageNavPages.ChangePasswordNavClass(ViewContext)" style="width: 300px" id="change-password" asp-page="./ChangePassword">Password</a></li>
    @if (hasExternalLogins)
    {
        <li id="external-logins" class="nav-item"><a id="external-login" class="nav-link @ManageNavPages.ExternalLoginsNavClass(ViewContext)" asp-page="./ExternalLogins">External logins</a></li>
    }
    <li class="nav-item"><a class="btn btn-outline-light @ManageNavPages.TwoFactorAuthenticationNavClass(ViewContext)" style="width: 300px" id="two-factor" asp-page="./TwoFactorAuthentication">Two-factor authentication</a></li>
    <li class="nav-item"><a class="btn btn-outline-light @ManageNavPages.PersonalDataNavClass(ViewContext)" style="width: 300px" id="personal-data" asp-page="./PersonalData">Personal data</a></li>
</ul>
